import React from 'react';

const ErrorPage = () => {
    return (
        <div>
            <h1 style={{color: 'red'}}>Sorry, but requested page doesn't exist!</h1>
        </div>
    );
};

export default ErrorPage;